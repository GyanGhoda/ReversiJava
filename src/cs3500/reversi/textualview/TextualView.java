package cs3500.reversi.textualview;

/**
 * Represents a textual view of a Reversi game board.
 */
public interface TextualView {
  // No methods as of yet. toString is used for now to render the board.
  // This is because a lack of a controller means that there is no need
  // for a method such as render(Appendable append) or any other such
  // method other than toString as of now.
}
